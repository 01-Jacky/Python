Simple hash table implementation with fixed size (per prompt).

To run:
1) Have a python 3 interpreter installed
2) Unzip content
3) Run driver.py to play with hashtable.
   test_hashtable.py contains some unit testing.
